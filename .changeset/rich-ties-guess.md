---
---

chore: bootstrap Changesets. The 1.1.0 release is being prepared manually
via the existing `publish.yml` (tag-triggered). From 1.1.1 onward, every
release goes through the Changesets flow: add a changeset on each PR,
merge the "Version Packages" PR to publish.
